<section id="resident">
<% @residents.each do |resident| %>
  <p><%= "Name: #{resident.name}, Age: #{resident.age}, Occupation: #{resident.occupation}" %></p>
<% end %>
<section id ="average_age">
<%= "Average Age: #{Resident.average_age}" %>